---
phase: 02-activity-editing-with-safety
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/automation/__init__.py
  - src/automation/exceptions.py
autonomous: false

must_haves:
  truths:
    - "P6ActivityEditor and P6RelationshipManager are importable from src.automation"
    - "P6EditError exception exists for edit-specific failures"
    - "All Phase 2 modules verified working against live P6 Professional 20"
  artifacts:
    - path: "src/automation/__init__.py"
      provides: "Updated package exports including new Phase 2 classes"
      contains: "P6ActivityEditor"
    - path: "src/automation/exceptions.py"
      provides: "P6EditError exception class"
      contains: "class P6EditError"
  key_links:
    - from: "src/automation/__init__.py"
      to: "src/automation/activity_editor.py"
      via: "Import and re-export P6ActivityEditor"
      pattern: "from .activity_editor import P6ActivityEditor"
    - from: "src/automation/__init__.py"
      to: "src/automation/relationships.py"
      via: "Import and re-export P6RelationshipManager"
      pattern: "from .relationships import P6RelationshipManager"
---

<objective>
Integrate all Phase 2 modules into the automation package and verify against live P6 Professional 20.

Purpose: Package integration ensures all new classes are discoverable via standard imports. Live verification confirms the GUI automation actually works against P6's Java Swing UI via UIA backend, catching any control identification issues before moving to Phase 3.

Output: Updated `__init__.py` and `exceptions.py`, plus live P6 verification results.
</objective>

<execution_context>
@C:\Users\ckr_4\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ckr_4\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-activity-editing-with-safety/02-RESEARCH.md
@.planning/phases/02-activity-editing-with-safety/02-01-SUMMARY.md
@.planning/phases/02-activity-editing-with-safety/02-02-SUMMARY.md
@.planning/phases/02-activity-editing-with-safety/02-03-SUMMARY.md
@src/automation/__init__.py
@src/automation/exceptions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add P6EditError exception and update package exports</name>
  <files>src/automation/exceptions.py, src/automation/__init__.py</files>
  <action>
**1. Add P6EditError to exceptions.py:**

Add after P6ExportError:
```python
class P6EditError(P6AutomationError):
    """Failed to edit activity data in P6."""
    pass
```

**2. Update __init__.py imports:**

Add these imports:
```python
from .activity_editor import P6ActivityEditor
from .relationships import P6RelationshipManager
```

Add P6EditError to the exceptions import block:
```python
from .exceptions import (
    ...,
    P6EditError
)
```

**3. Update __all__ list:**

Add to appropriate sections:
- Core classes: `'P6ActivityEditor'`, `'P6RelationshipManager'`
- Exceptions: `'P6EditError'`

Keep all existing exports unchanged.
  </action>
  <verify>
Run import checks:
```bash
python -c "
from src.automation import P6ActivityEditor, P6RelationshipManager, P6EditError
from src.automation import P6ActivityManager, P6ExportManager  # existing still work
print('All imports OK')
"
```
  </verify>
  <done>
- P6EditError exception class added
- P6ActivityEditor importable from src.automation
- P6RelationshipManager importable from src.automation
- All existing exports still work
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 2 Activity Editing implementation:
1. P6ActivityEditor -- edit start/finish dates via Details Form, edit durations, verify all edits
2. P6RelationshipManager -- add/remove predecessor relationships via Relationships tab Assign dialog
3. P6ActivityManager.create_activity -- create activities under WBS with Details Form field filling
4. P6ExportManager.backup_to_xer -- on-demand XER backup to user-specified path
  </what-built>
  <how-to-verify>
With P6 Professional 20 running and a test project open (e.g., AB-001):

**Test 1: Activity Selection (baseline check)**
```python
from src.automation.base import P6AutomationBase
from src.automation.activities import P6ActivityManager

p6 = P6AutomationBase()
p6.connect()
am = P6ActivityManager(p6._window, safe_mode=False)
result = am.select_activity("A1010")  # Use a known Activity ID
print(f"Select activity: {result}")
```

**Test 2: Edit Start Date**
```python
from src.automation.activity_editor import P6ActivityEditor

editor = P6ActivityEditor(p6._window, am, safe_mode=False)
result = editor.edit_start_date("A1010", "15-Mar-26")
print(f"Edit start date: {result}")
# Visually confirm in P6 that the date changed
```

**Test 3: Edit Duration**
```python
result = editor.edit_duration("A1010", 10)
print(f"Edit duration: {result}")
# Visually confirm in P6 that duration changed
```

**Test 4: Add Predecessor (if safe to do so on test data)**
```python
from src.automation.relationships import P6RelationshipManager

rm = P6RelationshipManager(p6._window, am, safe_mode=False)
result = rm.add_predecessor("A1020", "A1010", rel_type="FS", lag=0)
print(f"Add predecessor: {result}")
# Visually confirm relationship appears in P6 Relationships tab
```

**Test 5: XER Backup**
```python
from src.automation.exporting import P6ExportManager
from pathlib import Path

em = P6ExportManager(p6._window)
backup_path = Path("C:/temp/test_backup.xer")
result = em.backup_to_xer(backup_path)
print(f"XER backup: {result}")
# Confirm file exists at C:/temp/test_backup.xer
```

Report which tests passed, which failed, and any error messages. Fix any issues found during live testing (same approach as Phase 1 Plan 03 -- auto-fix bugs, document deviations).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found during testing</resume-signal>
</task>

</tasks>

<verification>
- All new classes importable from src.automation package
- P6EditError exception available
- Live P6 verification covers: activity selection, date editing, duration editing, relationship adding, XER backup
- Any bugs found during live testing are fixed and documented
</verification>

<success_criteria>
- All Phase 2 modules integrated into automation package
- Live P6 Professional 20 verification confirms date/duration editing works through Details Form
- Relationship management tested against live P6
- XER backup creates valid file at user-specified path
- All Phase 2 success criteria from ROADMAP.md are met
</success_criteria>

<output>
After completion, create `.planning/phases/02-activity-editing-with-safety/02-04-SUMMARY.md`
</output>
